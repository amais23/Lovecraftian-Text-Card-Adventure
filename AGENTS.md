# AGENTS.md

## Agent skills

### Issue tracker

本專案的工作任務（Issues）與規格需求（PRDs）追蹤於 GitHub Issues。詳見 `docs/agents/issue-tracker.md`。

### Triage labels

採用標準的五種分流標籤（`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`）。詳見 `docs/agents/triage-labels.md`。

### Domain docs

採用單一領域架構（根目錄下的 `CONTEXT.md` 與 `docs/adr/` 架構決策記錄）。詳見 `docs/agents/domain.md`。
